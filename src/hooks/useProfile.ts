import { useApiQuery } from './useApiQuery';import { earnService } from '../services/api/earn.service';import { userService } from '../services/api/user.service';import { useAuth } from '../context/AuthContext';
export function useProfile(){const {user}=useAuth();const summary=useApiQuery(()=>userService.getProfileSummary(),null,[]);const profileUser={name:user?.name??'',handle:`@${user?.name?.toLowerCase().replace(/\s/g,'')??'user'}`,avatar:user?.avatar??'',level:user?.level??1,levelProgress:((user?.xp??0)%500)/5,points:user?.coins?.toLocaleString()??'0'};const profileStats=[{label:'Posts',value:(summary.data?.posts??0).toLocaleString()},{label:'Following',value:(summary.data?.following??0).toLocaleString()},{label:'Followers',value:(summary.data?.followers??0).toLocaleString()}];const week=summary.data?.week;const weekStats=[{label:'Calories',value:(week?.calories??0).toLocaleString(),sub:`/${((user?.goal_kcal??0)*7).toLocaleString()} kcal`,percent:user?.goal_kcal?Math.round((week?.calories??0)/(user.goal_kcal*7)*100):0},{label:'Steps',value:(week?.steps??0).toLocaleString(),sub:`/${((user?.goal_steps??0)*7).toLocaleString()}`,percent:user?.goal_steps?Math.round((week?.steps??0)/(user.goal_steps*7)*100):0},{label:'Workouts',value:(week?.workouts??0).toString(),sub:'this week',percent:Math.min(100,(week?.workouts??0)*20)}];return{profileUser,profileStats,weekStats,loading:summary.loading};}
export function useRewards(){const earn=useApiQuery(()=>earnService.getEntries(),null,[]);const iconFor=(source:string):any=>source==='workout'?'barbell-outline':source==='meal-log'?'restaurant-outline':source==='challenge'?'trophy-outline':source==='referral'?'person-add-outline':'star-outline';const rewardsList=(earn.data?.entries??[]).map(e=>({id:e.id,icon:iconFor(e.source),label:e.label,points:`+${e.amount}`,done:true}));return{rewardsPoints:(earn.data?.total??0).toLocaleString(),rewardsList,loading:earn.loading};}
export function useInvite(){const {user}=useAuth();return{inviteCode:user?.referral_code??'',loading:false};}
