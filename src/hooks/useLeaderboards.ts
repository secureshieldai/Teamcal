import { useAuth } from '../context/AuthContext';import { useApiQuery } from './useApiQuery';import { socialService, type LeaderboardUser } from '../services/api/social.service';
export function useLeaderboard(scope:'global'|'friends'|'teams') {const {user}=useAuth();const query=useApiQuery(()=>socialService.getLeaderboard(50,scope),[] as LeaderboardUser[],[scope]);const entries=query.data.map((u,i)=>({rank:i+1,name:u.name,avatar:u.avatar??'',points:`${(u.xp||0).toLocaleString()} pts`,highlighted:scope!=='teams'&&u.id===user?.id}));const yourRank=entries.find(e=>e.highlighted)||null;const top=entries[0];const highlightTeam=top?{name:top.name,rankLabel:scope==='teams'?'Top ranked team':'Leaderboard leader',points:top.points}:null;return{entries,yourRank,highlightTeam,loading:query.loading,refetch:query.refetch};}
