import React,{useState} from 'react';import {Alert,StyleSheet,Text,TextInput,TouchableOpacity,View} from 'react-native';import {SafeAreaView} from 'react-native-safe-area-context';import {Ionicons} from '@expo/vector-icons';import {useNavigation} from '@react-navigation/native';import {challengesService} from '../services/api/challenges.service';import {colors,radii,spacing,typography} from '../theme';
export default function CreateChallengeScreen(){const navigation=useNavigation();const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [days,setDays]=useState('7');const [photo,setPhoto]=useState('');const [busy,setBusy]=useState(false);const save=async()=>{if(!title.trim())return Alert.alert('Title required');try{setBusy(true);await challengesService.create({title:title.trim(),description:description.trim(),durationDays:Number(days)||7,photo:photo.trim()||undefined});navigation.goBack()}catch(e){Alert.alert('Unable to create challenge',(e as Error).message)}finally{setBusy(false)}};return <SafeAreaView style={s.safe}><View style={s.header}><TouchableOpacity onPress={()=>navigation.goBack()}><Ionicons name="close" size={24}/></TouchableOpacity><Text style={s.title}>Create Challenge</Text><View style={{width:24}}/></View><View style={s.content}>{[['Title',title,setTitle],['Description',description,setDescription],['Duration (days)',days,setDays],['Cover image URL',photo,setPhoto]].map(([l,v,f])=><View key={l as string}><Text style={s.label}>{l as string}</Text><TextInput style={[s.input,l==='Description'&&{minHeight:80}]} value={v as string} onChangeText={f as (x:string)=>void} multiline={l==='Description'} keyboardType={l==='Duration (days)'?'number-pad':'default'}/></View>)}<TouchableOpacity style={s.button} onPress={save} disabled={busy}><Text style={s.buttonText}>{busy?'Creating…':'Create Challenge'}</Text></TouchableOpacity></View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},header:{flexDirection:'row',justifyContent:'space-between',padding:spacing.lg},title:{...typography.h2},content:{padding:spacing.lg},label:{...typography.caption,color:colors.textSecondary,marginTop:spacing.md,marginBottom:spacing.sm},input:{backgroundColor:colors.card,borderWidth:1,borderColor:colors.border,borderRadius:radii.md,padding:spacing.md},button:{backgroundColor:colors.primary,borderRadius:radii.pill,padding:spacing.md,alignItems:'center',marginTop:spacing.xl},buttonText:{color:colors.white,fontWeight:'700'}});
