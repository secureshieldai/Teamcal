import React from 'react';import {ScrollView,StyleSheet,Text,TouchableOpacity,View} from 'react-native';import {SafeAreaView} from 'react-native-safe-area-context';import {Ionicons} from '@expo/vector-icons';import {useNavigation} from '@react-navigation/native';import {useApiQuery} from '../hooks/useApiQuery';import {marketplaceService} from '../services/api/marketplace.service';import {colors,radii,spacing,typography} from '../theme';
export default function MarketplaceOrdersScreen(){const navigation=useNavigation();const orders=useApiQuery(()=>marketplaceService.getOrders(),[],[]);return <SafeAreaView style={s.safe}><View style={s.header}><TouchableOpacity onPress={()=>navigation.goBack()}><Ionicons name="chevron-back" size={24}/></TouchableOpacity><Text style={s.title}>My Orders</Text><View style={{width:24}}/></View><ScrollView contentContainerStyle={s.content}>{orders.data.map(o=><View style={s.card} key={o.id}><Text style={s.name}>{o.items.map(x=>x.title).join(', ')}</Text><Text style={s.meta}>{new Date(o.created_at).toLocaleString()}</Text><Text style={s.price}>{o.currency.toUpperCase()} {(Number(o.total_amount)/100).toFixed(2)}</Text><Text style={s.status}>{o.status}</Text></View>)}{!orders.loading&&!orders.data.length&&<Text style={s.empty}>No orders yet.</Text>}</ScrollView></SafeAreaView>};
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},header:{flexDirection:'row',justifyContent:'space-between',padding:spacing.lg},title:{...typography.h2},content:{padding:spacing.lg,gap:spacing.md},card:{backgroundColor:colors.card,padding:spacing.lg,borderRadius:radii.lg},name:{...typography.bodyBold},meta:{...typography.small,color:colors.textMuted,marginTop:3},price:{fontWeight:'800',color:colors.primary,marginTop:spacing.sm},status:{...typography.caption,color:colors.textSecondary,textTransform:'capitalize'},empty:{textAlign:'center',color:colors.textMuted,marginTop:40}});
